{
  "name": "paton-website",
  "version": "1.0.0",
  "private": true,
  "description": "paton.ch — marketing site for PATON, built with Eleventy and deployed to Firebase Hosting.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "clean": "rimraf public"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "rimraf": "^6.0.1"
  }
}
